═══════════════════════════════════════════════════════════════════════════════
 GENERIC TREE - QUICK COMMANDS REFERENCE
═══════════════════════════════════════════════════════════════════════════════

This file contains all essential commands for building, testing, and uploading
the Generic Tree package to PyPI.

═══════════════════════════════════════════════════════════════════════════════
 📦 SETUP & INSTALLATION
═══════════════════════════════════════════════════════════════════════════════

# Install development dependencies
pip install -e ".[dev]"

# Install build tools
pip install build twine wheel setuptools

# Install testing tools
pip install pytest pytest-cov tox black isort flake8 mypy pylint

═══════════════════════════════════════════════════════════════════════════════
 🧪 TESTING COMMANDS
═══════════════════════════════════════════════════════════════════════════════

# Run all tests
pytest

# Run specific test file
pytest test_generic_tree.py

# Run with verbose output
pytest -v

# Run with coverage report
pytest --cov=generic_tree

# Run exhaustive tests
pytest test_generic_tree_exhaustive.py

# Run multi-version tests
tox

# Run tests for specific Python version
tox -e py39

═══════════════════════════════════════════════════════════════════════════════
 🎨 CODE QUALITY COMMANDS
═══════════════════════════════════════════════════════════════════════════════

# Format code with Black
black .

# Check code formatting
black --check .

# Sort imports with isort
isort .

# Check import sorting
isort --check-only .

# Lint code with flake8
flake8 .

# Lint code with pylint
pylint generic_tree

# Type check with mypy
mypy generic_tree

# Run all code quality checks
black --check . && isort --check-only . && flake8 . && mypy generic_tree

═══════════════════════════════════════════════════════════════════════════════
 🔨 BUILDING COMMANDS
═══════════════════════════════════════════════════════════════════════════════

# Build using automated script
python build.py

# Build using bash (Linux/Mac)
bash build.sh

# Build using batch (Windows)
build.bat

# Build using direct Python command
python -m build

# Clean previous builds
rm -rf build/ dist/ *.egg-info  # Linux/Mac
rmdir /s build dist *.egg-info  # Windows

# Verify build
python -m twine check dist/*

═══════════════════════════════════════════════════════════════════════════════
 📤 UPLOADING COMMANDS
═══════════════════════════════════════════════════════════════════════════════

# Upload using automated script
bash upload.sh        # Linux/Mac
upload.bat            # Windows

# Upload to TestPyPI
python -m twine upload --repository testpypi dist/*

# Upload to production PyPI
python -m twine upload dist/*

# Verify upload on PyPI
# Visit: https://pypi.org/project/generic-tree/

═══════════════════════════════════════════════════════════════════════════════
 ✅ POST-UPLOAD VERIFICATION
═══════════════════════════════════════════════════════════════════════════════

# Test installation from PyPI
pip install generic-tree

# Quick sanity check
python -c "from generic_tree import Tree, TreeNode, TraversalMode; print('✓ OK')"

# Check installed version
pip show generic-tree

# Uninstall if needed
pip uninstall generic-tree

═══════════════════════════════════════════════════════════════════════════════
 🔐 PyPI ACCOUNT SETUP
═══════════════════════════════════════════════════════════════════════════════

# Register account (web)
https://pypi.org/account/register/

# Create ~/.pypirc file (Linux/Mac) or %APPDATA%\pip\pip.ini (Windows)
[distutils]
index-servers = pypi

[pypi]
  repository = https://upload.pypi.org/legacy/
  username = __token__
  password = pypi-YOUR_TOKEN_HERE

# After creating .pypirc, secure it
chmod 600 ~/.pypirc  # Linux/Mac

═══════════════════════════════════════════════════════════════════════════════
 🚀 FULL WORKFLOW EXAMPLE
═══════════════════════════════════════════════════════════════════════════════

# 1. Make changes to code
# ...edit generic_tree.py...

# 2. Run tests to verify changes
pytest -v

# 3. Run code quality checks
black . && isort . && flake8 . && mypy generic_tree

# 4. Update version in setup.py and pyproject.toml
# ...update version numbers...

# 5. Update CHANGELOG.md
# ...add release notes...

# 6. Build distribution
python build.py

# 7. Verify build
python -m twine check dist/*

# 8. Test install locally (optional)
pip install dist/generic_tree-1.0.0-py3-none-any.whl

# 9. Upload to PyPI
python -m twine upload dist/*

# 10. Create git tag
git tag v1.0.0
git push origin v1.0.0

═══════════════════════════════════════════════════════════════════════════════
 ⚠️  TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════════════

# If tests fail
pytest -v --tb=long  # See detailed error messages

# If build fails
rm -rf build/ dist/ *.egg-info && python build.py  # Clean rebuild

# If upload fails
python -m twine check dist/*  # Verify package format
# Check ~/.pypirc credentials

# If import fails after install
pip install --force-reinstall generic-tree

# Check PyPI credentials
# Visit: https://pypi.org/account/api-tokens/

═══════════════════════════════════════════════════════════════════════════════
 📋 USEFUL LINKS
═══════════════════════════════════════════════════════════════════════════════

PyPI Website: https://pypi.org/
Your Package: https://pypi.org/project/generic-tree/
PyPI Help: https://pypi.org/help/
Package Guide: https://packaging.python.org/

═══════════════════════════════════════════════════════════════════════════════
